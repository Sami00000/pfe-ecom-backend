nelmio_cors:
    defaults:
        allow_origin: ['*']
        allow_headers: ['content-type']
        allow_methods: ['POST', 'GET', 'OPTIONS', 'PUT', 'DELETE']
        max_age: 3600
    paths:
        '^/api/': ~
